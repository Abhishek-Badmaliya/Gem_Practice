<h1 class="text-center">User Listing Page</h1><br>
<% @users.each do |user| %>
  <%= user.email %>
  <%= user.roles.pluck(:name) %>
  <b>Current user can see users index?</b>
  <%= policy(User).index? %>
  <b>Current user can edit roles?</b>
  <%= policy(User).edit? %>
  <%= link_to "Edit Roles", edit_user_path(user) if policy(User).edit? %>
  <%= user.posts.pluck(:id) %>
  <%= user.creator_posts.pluck(:id) %>
  <%= user.editor_posts.pluck(:id) %>
  <br>
<% end %>
